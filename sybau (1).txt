wait(1) 
getgenv().Hide_UI = false

local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")

local utils = {}
function utils.create(class, prop)
    local obj = Instance.new(class)
    for k, v in pairs(prop) do obj[k] = v end
    return obj
end

local StatusLabel

if not getgenv().Hide_UI then
    if not game:IsLoaded() then game.Loaded:Wait() end
    pcall(function()
        for _, v in pairs(CoreGui:GetChildren()) do
            if v:IsA("ScreenGui") and v.Name == "abcyohkh#%$>][][][]" then v:Destroy() end
        end
    end)

    local ScreenGui = utils.create("ScreenGui", {
        Name = "abcyohkh#%$>][][][]",
        Parent = CoreGui,
        IgnoreGuiInset = true,
        ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    })

    local Frame = utils.create("Frame", {
        Parent = ScreenGui,
        BackgroundTransparency = 1,
        Size = UDim2.new(1, 0, 1, 0)
    })

    local Banner = utils.create("Frame", {
        Parent = Frame,
        AnchorPoint = Vector2.new(0, 0),
        Position = UDim2.new(0.05, 0, 0.12, 0), -- góc trái dễ nhìn
        Size = UDim2.new(0, 280, 0, 55),
        BackgroundColor3 = Color3.fromRGB(0, 0, 0),
        BorderColor3 = Color3.fromRGB(255, 255, 255),
        BorderSizePixel = 2,
        BackgroundTransparency = 0.05,
        ZIndex = 10
    })

    local Logo = utils.create("ImageLabel", {
        Parent = Banner,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0.04, 0, 0.5, 0),
        Size = UDim2.new(0, 40, 0, 40),
        BackgroundTransparency = 1,
        Image = "rbxassetid://111223180842676",
        ZIndex = 11
    })

    StatusLabel = utils.create("TextLabel", {
        Parent = Banner,
        AnchorPoint = Vector2.new(0, 0.5),
        Position = UDim2.new(0.26, 0, 0.5, 0),
        Size = UDim2.new(0.7, 0, 0.7, 0),
        BackgroundTransparency = 1,
        Font = Enum.Font.GothamBold,
        Text = "No.1Hub",
        TextColor3 = Color3.fromRGB(255, 255, 255),
        TextScaled = true,
        ZIndex = 12
    })

    local Timer = utils.create("TextLabel", {
        Parent = Frame,
        AnchorPoint = Vector2.new(0, 0),
        Position = UDim2.new(0.055, 0, 0.195, 0),
        Size = UDim2.new(0, 280, 0, 25),
        BackgroundTransparency = 1,
        Font = Enum.Font.FredokaOne,
        TextColor3 = Color3.fromRGB(180, 180, 180),
        TextScaled = true,
        Text = "0 Hours 0 Minutes 0 Seconds",
        ZIndex = 9
    })

    local startTime = tick()
    RunService.Heartbeat:Connect(function()
        local elapsed = tick() - startTime
        Timer.Text = string.format("%d Hours %d Minutes %d Seconds",
            math.floor(elapsed / 3600),
            math.floor((elapsed % 3600) / 60),
            math.floor(elapsed % 60))
    end)
end

local function updateStatus(msg, color)
    if StatusLabel then
        StatusLabel.Text = tostring(msg)
        StatusLabel.TextColor3 = color or Color3.fromRGB(255, 255, 255)
    end
end

wait(1)
updateStatus("None", Color3.fromRGB(255, 255, 255))

local function HasSkullGuitar()
    local p = game.Players.LocalPlayer
    if not p then return false end
    if p.Backpack:FindFirstChild("Skull Guitar") then return true end
    if p.Character and p.Character:FindFirstChild("Skull Guitar") then return true end
    if p.PlayerGui.Main.Stash:FindFirstChild("Skull Guitar", true) then return true end
    return false
end

spawn(function()
    while task.wait(1) do
        pcall(function()
            if HasSkullGuitar() then
                getgenv().Config["Start Get Skull Guitar"] = false
                getgenv().Config["Auto Farm Material"] = false
                updateStatus("Skull Guitar Obtained", Color3.fromRGB(255, 255, 255))
            end
        end)
    end
end)

task.spawn(function()
    task.wait(2)
    local team = getgenv().Config["Team"]
    if team == "Marines" or team == "Pirates" then
        pcall(function()
            ReplicatedStorage.Remotes.CommF_:InvokeServer("SetTeam", team)
        end)
    end
end)

wait(5)

local p = game.Players.LocalPlayer
repeat wait() until game:IsLoaded()
local rs = game.ReplicatedStorage
local w = workspace
local Sec = 0.2

pcall(function() rs.Remotes.CommF_:InvokeServer("SetTeam", getgenv().Config.Team) end)

local function hrp() return p.Character and p.Character:FindFirstChild("HumanoidRootPart") end

local function Alive()
    local c = p.Character
    if not c then return false end
    local h = c:FindFirstChildWhichIsA("Humanoid")
    return h and h.Health > 0 and c:FindFirstChild("HumanoidRootPart")
end

local function tween(cf)
    local h = hrp()
    if not h then return end

    for _,v in pairs(p.Character:GetDescendants()) do 
        if v:IsA("BasePart") then 
            v.CanCollide = false
        end 
    end

    local bv = Instance.new("BodyVelocity", h)
    bv.MaxForce = Vector3.new(1e9, 1e9, 1e9)

    local speed = getgenv().Config["Tween Speed"]
    local target = cf.Position

    while Alive() and (h.Position - target).Magnitude > 3 and getgenv().Config["Start Get Skull Guitar"] do
        local dir = (target - h.Position).Unit
        bv.Velocity = dir * speed
        task.wait()
    end

    h.CFrame = cf
    bv:Destroy()
end

World1 = game.PlaceId == 2753915549
World2 = game.PlaceId == 4442272183
World3 = game.PlaceId == 7449423635

local function TravelSea(n)
    if n == 2 and not World2 then rs.Remotes.CommF_:InvokeServer("TravelDressrosa") end
    if n == 3 and not World3 then rs.Remotes.CommF_:InvokeServer("TravelZou") end
end

local plr = game.Players.LocalPlayer

local function waitForChar()
    if not plr.Character then
        plr.CharacterAdded:Wait()
    end
    return plr.Character, plr.Character:WaitForChild("HumanoidRootPart", 5)
end

local function GetWP(name)
    for _, v in ipairs(p.Backpack:GetChildren()) do
        if v.Name == name then return true end
    end
    for _, v in ipairs((p.Character or {}):GetChildren()) do
        if v.Name == name then return true end
    end

    local inv = rs.Remotes.CommF_:InvokeServer("getInventory")
    if inv then
        for _, item in ipairs(inv) do
            if item.Name == name then
                return true
            end
        end
    end

    return false
end

task.spawn(function()
    while task.wait(1) do
        pcall(function()
            local char, hrp = waitForChar()
            local humanoid = char:FindFirstChildOfClass("Humanoid")
            if not humanoid then return end

            local equipped = char:FindFirstChildOfClass("Tool")
            local foundTool

            for _, tool in ipairs(plr.Backpack:GetChildren()) do
                if tool:IsA("Tool") then
                    for _, name in ipairs(getgenv().Config["Weapon"]) do
                        if string.lower(tool.ToolTip) == string.lower(name) then
                            foundTool = tool
                            break
                        end
                    end
                end
            end

            if foundTool and (not equipped or equipped.ToolTip ~= foundTool.ToolTip) then
                humanoid:EquipTool(foundTool)
            end

            if not char:FindFirstChild("HasBuso") then
                pcall(function()
                    ReplicatedStorage.Remotes.CommF_:InvokeServer("Buso")
                end)
            end
        end)
    end
end)

spawn(function()
    while task.wait() do
        pcall(function()
            local char = game.Players.LocalPlayer.Character
            if char then
                for _, v in pairs(char:GetDescendants()) do
                    if v:IsA("BasePart") and v.CanCollide == true then
                        v.CanCollide = false
                    end
                end
            end
        end)
    end
end)

local function GetM(name)
    local inv = rs.Remotes.CommF_:InvokeServer("getInventory")
    if inv then
        for _, item in ipairs(inv) do
            if item.Name == name then
                return item.Count or item.Amount or 0
            end
        end
    end

    local stash = rs.Remotes.CommF_:InvokeServer("getInventory", true)
    if stash then
        for _, item in ipairs(stash) do
            if item.Name == name then
                return item.Count or item.Amount or 0
            end
        end
    end

    return 0
end

local function GetEnemy(list)
    for _,v in pairs(w.Enemies:GetChildren()) do
        if table.find(list,v.Name) and v:FindFirstChild("HumanoidRootPart") and v.Humanoid.Health > 0 then return v end
    end
end

local function Kill(m)
    if not m or not Alive() then return end
    repeat 
        task.wait()
        if not Alive() then break end
        tween(m.HumanoidRootPart.CFrame * CFrame.new(0,7,0))
    until not m.Parent or m.Humanoid.Health <= 0 or not getgenv().Config["Start Get Skull Guitar"]
end

spawn(function()
    while task.wait(Sec) do
    if not Alive() then
        repeat task.wait() until Alive()
    end
        if getgenv().Config["Auto Farm Material"] and getgenv().Config["Start Get Skull Guitar"] and not GetWP("Skull Guitar") then

            if GetM("Ectoplasm") < 250 then
                updateStatus("Farming Ectoplams", Color3.fromRGB(255, 255, 255))
                TravelSea(2)
                local m = GetEnemy({"Ship Deckhand","Ship Engineer","Ship Steward","Ship Officer"})
                if m then Kill(m) else rs.Remotes.CommF_:InvokeServer("requestEntrance",Vector3.new(923,127,32852)) end

            elseif GetM("Bones") < 500 then
                updateStatus("Farming Bones", Color3.fromRGB(255, 255, 255))
                TravelSea(3)
                local m = GetEnemy({"Reborn Skeleton","Living Zombie","Demonic Soul","Posessed Mummy"})
                if m then Kill(m) else tween(CFrame.new(-9504,172,6057)) end

            elseif GetM("Bones") >= 500 and GetM("Ectoplasm") >= 250 then
                updateStatus("Buying Skull Guitar", Color3.fromRGB(255, 255, 255))
                rs.Remotes.CommF_:InvokeServer("soulGuitarBuy",true)
                updateStatus("Skull Guitar Obtained", Color3.fromRGB(255, 255, 255))
            end
        end
    end
end)

_G.BringMob = function(pos, name)
    _G.PosMon = pos
    _G.NameMon = name
end

local MaxBring = 6
local BringRange = 250
local lp = game.Players.LocalPlayer

local function IsTarget(mob)
    return mob
        and mob.Parent
        and mob:FindFirstChild("Humanoid")
        and mob:FindFirstChild("HumanoidRootPart")
        and mob.Humanoid.Health > 0
        and mob.Name == _G.NameMon
end

task.spawn(function()
    while task.wait(0.5) do
        if _G.BringMonster and _G.PosMon and _G.NameMon and lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
            local count = 0
            for _, mob in ipairs(workspace.Enemies:GetChildren()) do
                if IsTarget(mob) then
                    if count >= MaxBring then break end
                    count += 1

                    local hrp = mob.HumanoidRootPart
                    hrp.CFrame = _G.PosMon * CFrame.new(math.random(-3,3), 0, math.random(-3,3))
                    hrp.CanCollide = false

                    local h = mob.Humanoid
                    h.WalkSpeed = 0
                    h.JumpPower = 0
                    if h:FindFirstChild("Animator") then
                        h.Animator:Destroy()
                    end

                    if not hrp:FindFirstChild("Lock") then
                        local bv = Instance.new("BodyVelocity", hrp)
                        bv.Name = "Lock"
                        bv.MaxForce = Vector3.new(1e9,1e9,1e9)
                        bv.Velocity = Vector3.new(0,0,0)
                    end

                    sethiddenproperty(lp, "SimulationRadius", math.huge)
                end
            end
        end
    end
end)

task.spawn(function()
  while task.wait() do
    if getgenv().Config["Start Get Skull Guitar"] then 
      local v = GetEnemy({"Living Zombie"})
      if v then 
        v.HumanoidRootPart.CFrame = CFrame.new(-10138,138,5902)
        v.HumanoidRootPart.CanCollide = false
        v.Humanoid.WalkSpeed = 0
        v.Humanoid.JumpPower = 0
      end    
    end
  end
end)

local function getT(n) return w.Map["Haunted Castle"].Tablet["Segment"..n].Line.Rotation.Z end
local function getRT(n) return w.Map["Haunted Castle"].Trophies.Quest["Trophy"..n].Handle.Rotation.Z end
local function Fire(n,s) fireclickdetector(w.Map["Haunted Castle"]["Placard"..n][s].ClickDetector) end

spawn(function()
    repeat task.wait() until getgenv().Config["Start Get Skull Guitar"] and World3
    while task.wait(Sec) do
    if not Alive() then
        repeat task.wait() until Alive()
    end
        local chk = rs.Remotes.CommF_:InvokeServer("GuitarPuzzleProgress","Check")

        if chk == nil then
            updateStatus("Solving Swamp Quest", Color3.fromRGB(255,200,200))
            tween(CFrame.new(-8655,141,6160))
            rs.Remotes.CommF_:InvokeServer("gravestoneEvent",2)
            rs.Remotes.CommF_:InvokeServer("gravestoneEvent",2,true)

        elseif chk.Swamp == false then
            updateStatus("Solving Swamp Quest", Color3.fromRGB(255,200,200))
            _G.BringMonster = true
            _G.BringMob(CFrame.new(-10138,138,5902), "Living Zombie")
            local v = GetEnemy({"Living Zombie"})
            if v then 
                Kill(v) 
            else 
                tween(CFrame.new(-10170,138,5934)) 
            end

        elseif chk.Gravestones == false then
            _G.BringMonster = false
            updateStatus("Solving Gravestones", Color3.fromRGB(200,150,255))
            Fire("7","Left") Fire("6","Left") Fire("5","Left") Fire("4","Right") Fire("3","Left") Fire("2","Right") Fire("1","Right")

        elseif chk.Ghost == false then
            updateStatus("Talking to Ghost", Color3.fromRGB(255,255,200))
            rs.Remotes.CommF_:InvokeServer("GuitarPuzzleProgress","Ghost",true)

        elseif chk.Trophies == false then
            updateStatus("Aligning Trophies", Color3.fromRGB(200,255,200))
            tween(CFrame.new(-9532,6,6078))
            for a,b in pairs({{1,1},{3,3},{4,4},{7,7},{10,10}}) do
                repeat task.wait() fireclickdetector(w.Map["Haunted Castle"].Tablet["Segment"..b[2]].ClickDetector) until getRT(a) == getT(b[2])
            end

        elseif chk.Pipes == false then
            updateStatus("Fixing Pipe Lab", Color3.fromRGB(200,200,255))
            local L = w.Map["Haunted Castle"]["Lab Puzzle"].ColorFloor.Model
            local function c(x) tween(x.CFrame) fireclickdetector(x.ClickDetector) end
            c(L.Part3) c(L.Part4) c(L.Part4) c(L.Part4) c(L.Part6) c(L.Part6) c(L.Part8) c(L.Part10) c(L.Part10) c(L.Part10)
        end
    end
end)

local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LP = Players.LocalPlayer

local function GetChar()
    return LP.Character or LP.CharacterAdded:Wait()
end

local function GetRoot()
    local char = GetChar()
    return char:FindFirstChild("HumanoidRootPart")
end

local Players = game:GetService("Players")
local RS = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LP = Players.LocalPlayer

local function GetChar()
    return LP.Character or LP.CharacterAdded:Wait()
end

local function GetRoot()
    local char = GetChar()
    return char:FindFirstChild("HumanoidRootPart")
end

local Modules = RS:WaitForChild("Modules")
local CombatUtil = require(Modules:WaitForChild("CombatUtil"))
local RE_Attack = Modules.Net:WaitForChild("RE/RegisterAttack")

local HIT_FUNCTION
local RunHitDetection = CombatUtil.RunHitDetection
pcall(function()
    local env = getsenv(Modules.CombatUtil)
    if env and env._G and env._G.SendHitsToServer then
        HIT_FUNCTION = env._G.SendHitsToServer
    end
end)

local FastAttack = {}

function FastAttack:IsAlive(mob)
    return mob
        and mob:FindFirstChild("Humanoid")
        and mob.Humanoid.Health > 0
        and mob:FindFirstChild("HumanoidRootPart")
end

function FastAttack:GetTargets(radius)
    local res = {}
    local root = GetRoot()
    if not root then return res end

    local pos = root.Position
    for _, mob in ipairs(workspace.Enemies:GetChildren()) do
        if self:IsAlive(mob) then
            if (mob.HumanoidRootPart.Position - pos).Magnitude <= (radius or 60) then
                table.insert(res, mob)
            end
        end
    end
    return res
end

function FastAttack:GetHitbox(mob)
    local list = {
        "RightLowerArm","RightUpperArm","LeftLowerArm","LeftUpperArm",
        "RightHand","LeftHand","HumanoidRootPart","Head"
    }
    return mob:FindFirstChild(list[math.random(1,#list)]) or mob.HumanoidRootPart
end

function FastAttack:Attack()
    local char = GetChar()
    local tool = char:FindFirstChildOfClass("Tool")
    if not tool then return end

    if tool.ToolTip == "Blox Fruit" then
        local remote = tool:FindFirstChild("LeftClickRemote")
        if remote then
            remote:FireServer(Vector3.new(0,-500,0),1,true)
            remote:FireServer(false)
        end
        return
    end

    local targets = self:GetTargets(65)
    if #targets == 0 then return end

    local args = {[1]=nil,[2]={}}
    for _, mob in ipairs(targets) do
        local hit = self:GetHitbox(mob)
        if not args[1] then args[1] = hit end
        table.insert(args[2], {mob, hit})
    end

    RE_Attack:FireServer(0)
    if HIT_FUNCTION then
        HIT_FUNCTION(unpack(args))
    end
end

_G.FastAttackToggle = true
RunService.Heartbeat:Connect(function()
    if _G.FastAttackToggle then
        pcall(function()
            FastAttack:Attack()
        end)
    end
end)
